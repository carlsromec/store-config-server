server:
  port: 8010
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/carlsromec/store-data.git
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s0p0rt3